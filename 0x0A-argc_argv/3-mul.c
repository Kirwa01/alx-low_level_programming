#include <stdio.h>
#include "main.h"

/**
 * main - multiplies two numbers
 * @argv: arguments of vectors
 * @argc: arguments of  characters
 * Return: 0
 */
int main(int argc, char *argv[])
{
	if (argc != 3)
	{
		printf("ERROR\n");
		return (1);
	}
	int num1 = atoi(argv[1]);
	int num2 = atoi(argv[2]);

	printf("%:
}
