#include "main.h"

/**
 * _strlen - creates a string of characters
 * @s: character used to create a string
 * Return: returns a string
 */

int _strlen(char *s)
{
	int z;

	int count = 0;

	for (z = 0 ; s[z] != '\0' ; z++)
	count++;
	}

